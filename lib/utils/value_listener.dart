import 'package:flutter/material.dart';

class ValueListener {
  
  static  ValueNotifier<bool> isDark = ValueNotifier<bool>(true); //pasos definir el ValueNotifier la clase, definir donde la vas a utilizar y Hacer el cambio de la variable

}